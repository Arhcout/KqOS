LIB_SRC=$(shell find . -name '*.c')

LIBC_OBJ=$(LIB_SRC:%=$(BUILD_DIR)/%.libc.o)

LIBC_CFLAGS=$(CFLAGS) -DLIBC
LIBC_ASMFLAGS=$(ASMFLAGS)

LIBK_OBJ=$(LIB_SRC:%=$(BUILD_DIR)/%.libk.o)

LIBK_CFLAGS=$(CFLAGS) -DLIBK
LIBK_ASMFLAGS=$(ASMFLAGS)

